import { SpecialButtonDirective } from './special-button.directive';

describe('SpecialButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialButtonDirective();
    expect(directive).toBeTruthy();
  });
});
